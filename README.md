# Income Prediction & Client Analytics Platform

🚀 **Платформа для прогнозирования дохода клиентов и аналитики с веб-интерфейсом**

Полнофункциональное веб-приложение для прогнозирования дохода клиентов на основе машинного обучения с современным Vue.js фронтендом и FastAPI бэкендом.

## 📊 О проекте

Проект представляет собой систему для:
- **Прогнозирования дохода** клиентов с использованием модели XGBoost
- **Сегментации клиентов** на основе предсказанного дохода
- **Визуализации аналитики** через интуитивный веб-интерфейс
- **Рекомендации финансовых продуктов** в зависимости от сегмента клиента

## 🏗️ Архитектура
├── backend/ # FastAPI приложение с ML моделью

├── frontend/ # Vue.js веб-интерфейс

└── docker-compose.yml # Оркестрация контейнеров


## ⚡ Быстрый старт

### Предварительные требования
- Docker и Docker Compose
- Git

### Запуск проекта

1. **Клонируйте репозиторий:**
```bash
git clone <your-repo-url>
cd <project-directory>
```
2. **Настройте переменные окружения:**
```bash
cp .env-sample .env
```
# Отредактируйте .env при необходимости

3.**Запустите приложение:**
```bash
docker-compose up -d
```
4.**Откройте в браузере:**

Frontend: http://localhost:3000
Backend API: http://localhost:8000
API документация: http://localhost:8000/docs

Backend (FastAPI)
✅ REST API для прогнозирования дохода

✅ ML модель на основе XGBoost

✅ Валидация данных через Pydantic схемы

✅ CORS middleware для фронтенд-интеграции

✅ Мониторинг метрик модели (WMAE, точность)

✅ Документация API автоматическая (Swagger/OpenAPI)

Frontend (Vue.js)
✅ Интуитивный интерфейс для ввода данных клиента

✅ Визуализация прогнозов и сегментации

✅ Рекомендации продуктов на основе сегмента

✅ Мониторинг качества модели в реальном времени

✅ Адаптивный дизайн для всех устройств

📈 Модель машинного обучения
Метрики модели
Точность: 94.2%

WMAE (Weighted Mean Absolute Error): 45215.09

Количество признаков: 156 (полная модель), 6 (упрощенная)

Ключевые признаки
incomeValue - Декларируемый доход клиента

age - Возраст клиента

turn_cur_cr_avg_act_v2 - Средние обороты по текущим кредитам

hdb_bki_total_max_limit - Общий максимальный лимит по БКИ

salary_6to12m_avg - Средняя зарплата за последние 6-12 месяцев

🔧 API Endpoints
Основные endpoints:
GET / - Статус API

GET /health - Проверка здоровья сервиса

POST /predict_single - Прогноз для одного клиента

GET /model/status - Статус модели

GET /model/metrics - Метрики модели

GET /features/info - Информация о признаках

📁 Структура проекта
```bash
C:.
│   .env                    # Переменные окружения
│   .env-sample            # Шаблон переменных окружения
│   .gitignore
│   docker-compose.yml     # Конфигурация Docker Compose
│   README.md
├───backend
│   │   Dockerfile
│   │   hackathon_income_test.csv    # Тестовые данные
│   │   hackathon_income_train.csv   # Тренировочные данные
│   │   requirements.txt
│   │   submission_wmae_35069.csv    # Результаты модели
│   └───src
│       │   main.py        # Основное FastAPI приложение
│       │   model.py       # ML модель и обучение
│       └───models
│               xgboost_model.pkl    # Сохраненная модель
│
└───frontend
    │ 
    │   Dockerfile
    │   package.json
    │   vite.config.js
    └───src
    │ 
    │   App.vue        # Основной компонент
    │   main.js        # Точка входа Vue
    └───router
            index.js   # Маршрутизация Vue Router
```
